import { StyleSheet } from 'react-native';

import { purple, gray, white, red, orange, blue, lightPurp, pink, chestnut, mint, mud, darkOrange, matcha } from './consts'

export default StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: white,
    alignItems: 'center',
    justifyContent: 'center',
  },
  loading: {
    flex: 1,
  }
});
